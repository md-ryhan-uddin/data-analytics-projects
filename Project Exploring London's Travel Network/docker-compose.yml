version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: london_travel_postgres
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - london_travel_data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  london_travel_data:
